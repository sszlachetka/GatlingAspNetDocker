{
    max-duration = 20 second
    max-duration = ${?LOAD_TESTS_MAX_DURATION_SEC}

    search-and-fetch-items-details {
        nothing-for = 0 second
        ramp-users = 200
        ramp-users-during = 5 second
        interval = 1 second
    }

    batch-update-items {
        nothing-for = 0 second
        ramp-users = 150
        ramp-users-during = 5 second
        interval = 1 second
    }
}